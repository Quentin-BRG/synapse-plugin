interface:
  display_name: "Update a Collection"
  short_description: "Change or extend existing Synapse learning content"
  default_prompt: "Use $update-collection to change or extend existing Synapse learning content."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "synapse"
      description: "Synapse learning service"
      transport: "streamable_http"
      url: "https://synapse.quentin-berger.fr/mcp"
