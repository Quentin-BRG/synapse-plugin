interface:
  display_name: "Synapse onboarding"
  short_description: "Discover Synapse with a guided first Collection"
  default_prompt: "Use $onboarding to discover Synapse with a guided first Collection."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "synapse"
      description: "Synapse learning service"
      transport: "streamable_http"
      url: "https://synapse.quentin-berger.fr/mcp"
