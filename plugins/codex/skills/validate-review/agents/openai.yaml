interface:
  display_name: "Validate a review"
  short_description: "Assess observable work and update private progress"
  default_prompt: "Use $validate-review to assess observable work and update private progress."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "synapse"
      description: "Synapse learning service"
      transport: "streamable_http"
      url: "https://synapse.quentin-berger.fr/mcp"
