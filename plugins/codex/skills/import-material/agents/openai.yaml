interface:
  display_name: "Import learning material"
  short_description: "Turn supported material into assessable knowledge units"
  default_prompt: "Use $import-material to turn supported material into assessable knowledge units."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "synapse"
      description: "Synapse remote learning service"
      transport: "streamable_http"
      url: "https://synapse.quentin-berger.fr/mcp"
