interface:
  display_name: "Create a Collection"
  short_description: "Start a structured learning journey in Synapse"
  default_prompt: "Use $create-collection to start a structured learning journey in Synapse."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "synapse"
      description: "Synapse learning service"
      transport: "streamable_http"
      url: "https://synapse.quentin-berger.fr/mcp"
