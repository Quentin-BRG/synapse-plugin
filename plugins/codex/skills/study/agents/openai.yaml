interface:
  display_name: "Study with Synapse"
  short_description: "Study a Collection and record evidence-backed progress"
  default_prompt: "Help me study this Synapse Collection."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: mcp
      value: synapse
      description: "Synapse remote learning service"
      transport: streamable_http
      url: "https://synapse.quentin-berger.fr/mcp"
