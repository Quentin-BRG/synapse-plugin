interface:
  display_name: "Review with Synapse"
  short_description: "Review due knowledge and update private progress"
  default_prompt: "Use $review to review due knowledge and update private progress."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "synapse"
      description: "Synapse remote learning service"
      transport: "streamable_http"
      url: "https://synapse.quentin-berger.fr/mcp"
